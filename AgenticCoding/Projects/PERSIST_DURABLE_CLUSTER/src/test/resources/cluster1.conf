include "application-test.conf"

pekko {
  remote.artery {
    canonical {
      hostname = "127.0.0.1"
      port = 2551
    }
  }
  
  cluster {
    seed-nodes = ["pekko://ClusterSystem@127.0.0.1:2551"]
    roles = ["shard", "node1", "seed"]
    
    configuration-compatibility-check {
      enforce-on-join = off
    }
    
    jmx.multi-mbeans-in-same-jvm = on
  }
}